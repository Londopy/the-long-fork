"""Shared code for the-long-fork: the CHAIN.txt line format, the chain tree,
the tracker's outputs, and the helpers that run in forks. Standard library only,
so it runs unchanged on a GitHub runner or anyone's laptop (Python 3.8+)."""
